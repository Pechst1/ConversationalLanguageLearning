/** WP-72 — the terms of use. Public; also served by the API at /terms. */
import { LegalPage } from '@/components/legal/LegalPage';

export default function TermsPage() {
  return <LegalPage kind="terms" />;
}
