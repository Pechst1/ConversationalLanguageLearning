# Next.js Configuration
# Checklist:
# - [ ] Replace NEXTAUTH_SECRET with a long, random value that remains the same across restarts.
# - [ ] Store the secret securely so locally issued JWT sessions keep working after the Next.js process restarts.
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-here-make-it-long-and-random

# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
NEXT_PUBLIC_WS_URL=ws://localhost:8000
API_URL=http://localhost:8000

# Development
NODE_ENV=development